# docs/images

Screenshots referenced by the root `README.md`. Pending:

- `calibration-dashboard.png` — `soarm-dashboard-calibration` (soarm_sdk), the four-tab guided
  calibration workflow (tolerances, signs, ROM/zeros, review & save) with the 3-D FK view.
- `tamp-dashboard.png` — the `soarm_tamp` dashboard's TCP Plan or Pick & Place tab: a plan, its ghost-mesh
  preview, and the live mirror during execution.

Drop the PNGs in with these exact names and the root README's image table picks them up automatically —
no other changes needed. This file can go once both exist.
